# -*- coding: utf-8 -*-
"""木造壁量計算 — mgt の板要素から階ごとの存在壁量を拾い、壁量を検定する.

構造計算によらない木造 (令46条4項) の壁量チェックを、MIDAS のモデルを
そのまま使って行うためのタブ。

  ・存在壁量  … mgt の板要素のうち **材料名に「壁倍率」を含む** ものを拾い、
                 階ごと・X/Y方向ごとに Σ(壁長L[m] × 壁倍率β) を集計する
  ・必要壁量  … 画面で入力した層重量から地震力 (Z·Rt·Ai·C0) を求め、
                 必要壁量 = Qi / 1.96[kN/m] とする。
                 風圧力は見付面積 × 50[cm/m2] (係数は変更可)
  ・検定      … 検定比 = 必要壁量 / 存在壁量 (地震・風の大きい方で判定)

**既存モジュールは変更していない。** このパッケージだけで完結し、
app.py への追加は Blueprint の登録1行のみ (loadmap と同じ作法)。

  mgt_walls.py  mgt から耐力壁を読む (単位系判定・階割当・壁のグループ化)
  calc.py       必要壁量の算定と検定・CSV出力
  routes.py     Flask Blueprint (/api/wallqty_read, /api/wallqty_check)
"""
